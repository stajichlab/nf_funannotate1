# Nextflow run artifacts
work/
work.*/
funannotate_db/
.nextflow/
.nextflow.log*
logs/
# pipeline outputs
genome_annotation/
genome_annotation_training/
input_clean_genomes/
rnaseq_reads/
rnaseq_data/
results/
tables/
# test-run artifacts (keep tests/data fixtures)
tests/work/
tests/output/
tests/data/*.rnaseq_sra.csv
# ad-hoc real-genome smoke test scaffolding (not a repo fixture)
/realtest/
/realtest_run.log
# scratch copies
*.orig
# python
__pycache__/
*.pyc
# built Rust helper binaries (scripts/build_tools.sh)
tools/
# stale launch artifacts left over from the pre-flatten nextflow/ subdir layout
nextflow/
# run-time data assets in the launch dir (augustus config copy, protein evidence,
# sbt) — keep tracked code under lib/ (e.g. lib/*.groovy)
/lib/augustus/
/lib/*.faa
/lib/template.sbt
tables/
